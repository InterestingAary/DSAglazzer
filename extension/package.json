{
  "name": "dsa-revision-tracker-extension",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch",
    "package": "node build.js --package"
  },
  "devDependencies": {
    "archiver": "^7.0.1",
    "esbuild": "^0.20.0"
  }
}
